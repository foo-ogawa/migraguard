// Auto-generated by cli-contracts. Do not edit.
import { Command } from "commander";
import { CONTRACT_YAML, CONTRACT_JSON_STR } from "./contract.js";

export interface CommandHandlers {
  new: (name: string | undefined, options: { expandContract?: boolean; dryRun?: boolean }, parentOpts: Record<string, unknown>) => Promise<void>;
  apply: (options: { withDriftCheck?: boolean; fromBaseline?: boolean; tag?: string; dryRun?: boolean }, parentOpts: Record<string, unknown>) => Promise<void>;
  check: (options: Record<string, never>, parentOpts: Record<string, unknown>) => Promise<void>;
  squash: (options: { dryRun?: boolean }, parentOpts: Record<string, unknown>) => Promise<void>;
  lint: (options: { format?: string }, parentOpts: Record<string, unknown>) => Promise<void>;
  dump: (options: { dryRun?: boolean }, parentOpts: Record<string, unknown>) => Promise<void>;
  diff: (options: { format?: string }, parentOpts: Record<string, unknown>) => Promise<void>;
  status: (options: { format?: string }, parentOpts: Record<string, unknown>) => Promise<void>;
  resolve: (file: string | undefined, options: { dryRun?: boolean }, parentOpts: Record<string, unknown>) => Promise<void>;
  editable: (options: Record<string, never>, parentOpts: Record<string, unknown>) => Promise<void>;
  verify: (options: { all?: boolean; format?: string }, parentOpts: Record<string, unknown>) => Promise<void>;
  groupStatus: (group: string | undefined, options: Record<string, never>, parentOpts: Record<string, unknown>) => Promise<void>;
  baseline: (options: { keepSince?: string; dryRun?: boolean }, parentOpts: Record<string, unknown>) => Promise<void>;
  advance: (group: string | undefined, phase: string | undefined, status: string | undefined, options: { tag?: string; dryRun?: boolean }, parentOpts: Record<string, unknown>) => Promise<void>;
  applyPhase: (group: string | undefined, phase: string | undefined, options: { tag?: string; dryRun?: boolean }, parentOpts: Record<string, unknown>) => Promise<void>;
  gate: (options: { require?: string; forbid?: string; contractFile?: string }, parentOpts: Record<string, unknown>) => Promise<void>;
  deps: (options: { html?: string }, parentOpts: Record<string, unknown>) => Promise<void>;
  audit: (target: string | undefined, options: { adapter?: string; model?: string; failOn?: string; output?: string; reportFormat?: string; dryRun?: boolean; showPrompt?: boolean }, parentOpts: Record<string, unknown>) => Promise<void | string>;
  proposeExpandContract: (file: string | undefined, options: { outputDir?: string; adapter?: string; model?: string; failOn?: string; output?: string; reportFormat?: string; dryRun?: boolean; showPrompt?: boolean }, parentOpts: Record<string, unknown>) => Promise<void | string>;
  implement: (description: string | undefined, options: { outputDir?: string; adapter?: string; model?: string; failOn?: string; output?: string; reportFormat?: string; dryRun?: boolean; showPrompt?: boolean }, parentOpts: Record<string, unknown>) => Promise<void | string>;
  auditWorkflow: (options: { adapter?: string; model?: string; failOn?: string; output?: string; reportFormat?: string; dryRun?: boolean; showPrompt?: boolean }, parentOpts: Record<string, unknown>) => Promise<void | string>;
  explain: (options: { adapter?: string; model?: string; failOn?: string; output?: string; reportFormat?: string; dryRun?: boolean; showPrompt?: boolean }, parentOpts: Record<string, unknown>) => Promise<void | string>;
}

export function createProgram(
  handlers: CommandHandlers,
  version: string,
): Command {
  const program = new Command();
  program
    .name("migraguard")
    .version(version, "-V, --version")
    .description("Schema-aware deployment control for SQL migrations.");

  program.option("--no-color", "Disable ANSI color output. Also respects NO_COLOR env var.", false);
  program.option("-q, --quiet", "Suppress informational output. Does not suppress structured stdout or error messages.", false);
  program.option("-c, --config <path>", "Path to migraguard configuration file.");

  program
    .command("new")
    .description("Create a new migration SQL file with UTC timestamp.")
    .argument("<name>", "Migration name (alphanumeric and underscores).")
    .option("--expand-contract", "Create an expand/contract migration group (Class B).", false)
    .option("-n, --dry-run", "Show what would be created without writing files.", false)
    .action(async (name, opts, cmd) => {
      await handlers.new(name, opts, cmd.optsWithGlobals());
    });

  program
    .command("apply")
    .description("Apply pending migrations via native database CLI.")
    .option("--with-drift-check", "Check schema drift before apply and update dump after.", false)
    .option("--from-baseline", "Apply schema.sql first, then remaining migrations.", false)
    .option("--tag <value>", "Tag to record with applied migrations (e.g. commit hash, release tag).")
    .option("-n, --dry-run", "List pending migrations and SQL that would be executed without applying.", false)
    .action(async (opts, cmd) => {
      await handlers.apply(opts, cmd.optsWithGlobals());
    });

  program
    .command("check")
    .description("Verify metadata integrity (no DB connection required).")
    .action(async (opts, cmd) => {
      await handlers.check({}, cmd.optsWithGlobals());
    });

  program
    .command("squash")
    .description("Squash multiple new migration files into one.")
    .option("-n, --dry-run", "Show what would be squashed without writing files.", false)
    .action(async (opts, cmd) => {
      await handlers.squash(opts, cmd.optsWithGlobals());
    });

  program
    .command("lint")
    .description("Run built-in safety rules on migration files.")
    .option("--format <fmt>", "Output format for lint results.", "text")
    .action(async (opts, cmd) => {
      await handlers.lint(opts, cmd.optsWithGlobals());
    });

  program
    .command("dump")
    .description("Dump and normalize current DB schema.")
    .option("-n, --dry-run", "Output normalized schema to stdout without writing schema.sql.", false)
    .action(async (opts, cmd) => {
      await handlers.dump(opts, cmd.optsWithGlobals());
    });

  program
    .command("diff")
    .description("Show diff between current DB schema and saved schema.sql.")
    .option("--format <fmt>", "Output format for diff results.", "text")
    .action(async (opts, cmd) => {
      await handlers.diff(opts, cmd.optsWithGlobals());
    });

  program
    .command("status")
    .description("Show migration status (applied / pending / failed / skipped).")
    .option("--format <fmt>", "Output format for status results.", "text")
    .action(async (opts, cmd) => {
      await handlers.status(opts, cmd.optsWithGlobals());
    });

  program
    .command("resolve")
    .description("Mark a failed migration as skipped (requires human judgment).")
    .argument("<file>", "Failed migration file name to resolve.")
    .option("-n, --dry-run", "Show what would be resolved without writing to DB.", false)
    .action(async (file, opts, cmd) => {
      await handlers.resolve(file, opts, cmd.optsWithGlobals());
    });

  program
    .command("editable")
    .description("List migration files that are currently editable.")
    .action(async (opts, cmd) => {
      await handlers.editable({}, cmd.optsWithGlobals());
    });

  program
    .command("verify")
    .description("Verify migration idempotency using a shadow DB.")
    .option("--all", "Verify all migrations from scratch, not only pending.", false)
    .option("--format <fmt>", "Output format for verification results.", "text")
    .action(async (opts, cmd) => {
      await handlers.verify(opts, cmd.optsWithGlobals());
    });

  program
    .command("group-status")
    .description("Show migration group state (expand/contract phases).")
    .argument("[group]", "Specific group name. Shows all groups if omitted.")
    .action(async (group, opts, cmd) => {
      await handlers.groupStatus(group, {}, cmd.optsWithGlobals());
    });

  program
    .command("baseline")
    .description("Squash applied migrations into schema.sql baseline.")
    .option("--keep-since <file...>", "Keep migration files from this point forward.")
    .option("-n, --dry-run", "Show what would be baselined without writing files.", false)
    .action(async (opts, cmd) => {
      await handlers.baseline(opts, cmd.optsWithGlobals());
    });

  program
    .command("advance")
    .description("Record a phase state transition (for external executor).")
    .argument("<group>", "Migration group name.")
    .argument("<phase>", "Phase to advance.")
    .argument("<status>", "New status for the phase.")
    .option("--tag <value>", "Tag to record (e.g. commit hash, release tag).")
    .option("-n, --dry-run", "Show what would be recorded without writing to DB.", false)
    .action(async (group, phase, status, opts, cmd) => {
      await handlers.advance(group, phase, status, opts, cmd.optsWithGlobals());
    });

  program
    .command("apply-phase")
    .description("Apply a specific phase of a migration group via native CLI.")
    .argument("<group>", "Migration group name.")
    .argument("<phase>", "Phase to apply.")
    .option("--tag <value>", "Tag to record (e.g. commit hash, release tag).")
    .option("-n, --dry-run", "Show what would be applied without executing SQL.", false)
    .action(async (group, phase, opts, cmd) => {
      await handlers.applyPhase(group, phase, opts, cmd.optsWithGlobals());
    });

  program
    .command("gate")
    .description("Evaluate deployment gate conditions against migration group states.")
    .option("--require <condition...>", "Required schema state conditions. Syntax: group:<name>.<condition>")
    .option("--forbid <condition...>", "Forbidden schema state conditions. Syntax: group:<name>.<condition>")
    .option("--contract-file <path>", "JSON file with schema requirements.")
    .action(async (opts, cmd) => {
      await handlers.gate(opts, cmd.optsWithGlobals());
    });

  program
    .command("deps")
    .description("Analyze and display migration dependency graph.")
    .option("--html <path>", "Output as HTML file with interactive visualization. File is created or overwritten at the specified path. Parent directory must exist.")
    .action(async (opts, cmd) => {
      await handlers.deps(opts, cmd.optsWithGlobals());
    });

  program
    .command("audit")
    .description("Run LLM-based migration safety audit.")
    .argument("[target]", "Migration file or directory to audit. Defaults to pending migrations.")
    .option("-a, --adapter <name>", "SDK adapter to use for LLM execution.")
    .option("--model <name>", "LLM model override.")
    .option("--fail-on <level>", "Minimum severity that causes a non-zero exit.", "error")
    .option("-o, --output <file>", "Write result to a file instead of stdout.")
    .option("--report-format <fmt>", "Output format for the audit report.", "json")
    .option("-n, --dry-run", "Output the constructed prompt without calling the LLM API.", false)
    .option("--show-prompt", "Output the constructed prompt without calling the LLM API.", false)
    .action(async (target, opts, cmd) => {
      if (opts.showPrompt) {
        const prompt = await handlers.audit(target, opts, cmd.optsWithGlobals());
        if (typeof prompt === "string") process.stdout.write(prompt + "\n");
        return;
      }
      await handlers.audit(target, opts, cmd.optsWithGlobals());
    });

  program
    .command("propose-expand-contract")
    .description("Propose expand/contract migration group from unsafe DDL.")
    .argument("<file>", "Migration file to decompose into expand/contract phases.")
    .option("--output-dir <dir>", "Directory to write proposed phase files.")
    .option("-a, --adapter <name>", "SDK adapter to use.")
    .option("--model <name>", "LLM model override.")
    .option("--fail-on <level>", "Minimum severity that causes a non-zero exit.", "error")
    .option("-o, --output <file>", "Write result to a file instead of stdout.")
    .option("--report-format <fmt>", "Output format for the proposal report.", "json")
    .option("-n, --dry-run", "Output the constructed prompt without calling the LLM API.", false)
    .option("--show-prompt", "Output the constructed prompt without calling the LLM API.", false)
    .action(async (file, opts, cmd) => {
      if (opts.showPrompt) {
        const prompt = await handlers.proposeExpandContract(file, opts, cmd.optsWithGlobals());
        if (typeof prompt === "string") process.stdout.write(prompt + "\n");
        return;
      }
      await handlers.proposeExpandContract(file, opts, cmd.optsWithGlobals());
    });

  program
    .command("implement")
    .description("Generate production-safe migration SQL from a natural language description.")
    .argument("<description>", "Natural language description of the desired schema change.")
    .option("--output-dir <dir>", "Directory to write generated migration file(s).")
    .option("-a, --adapter <name>", "SDK adapter to use for LLM execution.")
    .option("--model <name>", "LLM model override.")
    .option("--fail-on <level>", "Minimum severity that causes a non-zero exit.", "error")
    .option("-o, --output <file>", "Write result to a file instead of stdout.")
    .option("--report-format <fmt>", "Output format for the implementation report.", "json")
    .option("-n, --dry-run", "Output the constructed prompt without calling the LLM API.", false)
    .option("--show-prompt", "Output the constructed prompt without calling the LLM API.", false)
    .action(async (description, opts, cmd) => {
      if (opts.showPrompt) {
        const prompt = await handlers.implement(description, opts, cmd.optsWithGlobals());
        if (typeof prompt === "string") process.stdout.write(prompt + "\n");
        return;
      }
      await handlers.implement(description, opts, cmd.optsWithGlobals());
    });

  program
    .command("audit-workflow")
    .description("Audit migration workflow compliance using LLM.")
    .option("-a, --adapter <name>", "SDK adapter to use for LLM execution.")
    .option("--model <name>", "LLM model override.")
    .option("--fail-on <level>", "Minimum severity that causes a non-zero exit.", "error")
    .option("-o, --output <file>", "Write result to a file instead of stdout.")
    .option("--report-format <fmt>", "Output format for the audit report.", "json")
    .option("-n, --dry-run", "Output the constructed prompt without calling the LLM API.", false)
    .option("--show-prompt", "Output the constructed prompt without calling the LLM API.", false)
    .action(async (opts, cmd) => {
      if (opts.showPrompt) {
        const prompt = await handlers.auditWorkflow(opts, cmd.optsWithGlobals());
        if (typeof prompt === "string") process.stdout.write(prompt + "\n");
        return;
      }
      await handlers.auditWorkflow(opts, cmd.optsWithGlobals());
    });

  program
    .command("explain")
    .description("Explain command output in human-readable form using LLM.")
    .option("-a, --adapter <name>", "SDK adapter to use.")
    .option("--model <name>", "LLM model override.")
    .option("--fail-on <level>", "Minimum severity that causes a non-zero exit.", "error")
    .option("-o, --output <file>", "Write result to a file instead of stdout.")
    .option("--report-format <fmt>", "Output format for the explanation report.", "json")
    .option("-n, --dry-run", "Output the constructed prompt without calling the LLM API.", false)
    .option("--show-prompt", "Output the constructed prompt without calling the LLM API.", false)
    .action(async (opts, cmd) => {
      if (opts.showPrompt) {
        const prompt = await handlers.explain(opts, cmd.optsWithGlobals());
        if (typeof prompt === "string") process.stdout.write(prompt + "\n");
        return;
      }
      await handlers.explain(opts, cmd.optsWithGlobals());
    });


  // Built-in extract command (auto-injected by cli-contracts)
  program
    .command("extract")
    .description("Extract contract specification for this CLI tool.")
    .argument("[commands...]", "Command IDs to extract. Use dot notation.")
    .option("-a, --all", "Extract all commands.", false)
    .option("--include-meta", "Include extraction metadata.", true)
    .option("-F, --format <format>", "Output format (yaml or json).", "yaml")
    .action(async (commands: string[], opts: { all?: boolean; includeMeta?: boolean; format?: string }, cmd: Command) => {
      if (commands.length === 0 && !opts.all) {
        process.stderr.write(JSON.stringify({ code: "INVALID_ARGS", message: "Specify command IDs or use --all" }) + "\n");
        process.exit(2);
      }

      const format = opts.format || "yaml";
      const doc = JSON.parse(CONTRACT_JSON_STR);
      const cmdIds = opts.all ? [] : commands;

      if (cmdIds.length === 0) {
        // --all: output full contract
        if (format === "json") {
          const out: Record<string, unknown> = {};
          if (opts.includeMeta) {
            out._meta = {
              source: "embedded",
              type: "cli-contracts/extract",
              extractedAt: new Date().toISOString(),
              specVersion: doc.cli_contracts ?? "0.1.0",
              commands: ["migraguard.new","migraguard.apply","migraguard.check","migraguard.squash","migraguard.lint","migraguard.dump","migraguard.diff","migraguard.status","migraguard.resolve","migraguard.editable","migraguard.verify","migraguard.group-status","migraguard.baseline","migraguard.advance","migraguard.apply-phase","migraguard.gate","migraguard.deps","migraguard.audit","migraguard.propose-expand-contract","migraguard.implement","migraguard.audit-workflow","migraguard.explain"],
            };
          }
          Object.assign(out, doc);
          process.stdout.write(JSON.stringify(out, null, 2) + "\n");
        } else {
          // YAML output
          const yamlLines: string[] = [];
          yamlLines.push("# migraguard extract");
          yamlLines.push("# source: embedded");
          yamlLines.push("# type: cli-contracts/command-extract");
          if (opts.includeMeta) {
            yamlLines.push("---");
            yamlLines.push("source: embedded");
            yamlLines.push("type: cli-contracts/command-extract");
            yamlLines.push("extractedAt: " + new Date().toISOString());
            yamlLines.push("spec_version: " + (doc.cli_contracts ?? "0.1.0"));
            yamlLines.push("commands:");
            for (const id of ["migraguard.new","migraguard.apply","migraguard.check","migraguard.squash","migraguard.lint","migraguard.dump","migraguard.diff","migraguard.status","migraguard.resolve","migraguard.editable","migraguard.verify","migraguard.group-status","migraguard.baseline","migraguard.advance","migraguard.apply-phase","migraguard.gate","migraguard.deps","migraguard.audit","migraguard.propose-expand-contract","migraguard.implement","migraguard.audit-workflow","migraguard.explain"]) {
              yamlLines.push("  - " + id);
            }
          }
          yamlLines.push("---");
          yamlLines.push(CONTRACT_YAML);
          process.stdout.write(yamlLines.join("\n") + "\n");
        }
      } else {
        // Filter specific commands
        const filtered: Record<string, unknown> = {
          cli_contracts: doc.cli_contracts,
          info: doc.info,
          command_sets: {},
        };
        const fcs = filtered.command_sets as Record<string, Record<string, unknown>>;
        for (const [setId, cs] of Object.entries(doc.command_sets ?? {})) {
          const cmds = (cs as Record<string, unknown>).commands as Record<string, unknown> | undefined;
          if (!cmds) continue;
          const matched: Record<string, unknown> = {};
          for (const [cmdId, cmdDef] of Object.entries(cmds)) {
            const fullId = setId + "." + cmdId;
            if (cmdIds.some((id) => id === cmdId || id === fullId || cmdId.startsWith(id + "."))) {
              matched[cmdId] = cmdDef;
            }
          }
          if (Object.keys(matched).length > 0) {
            const setCopy = { ...(cs as Record<string, unknown>) };
            setCopy.commands = matched;
            fcs[setId] = setCopy;
          }
        }
        if (doc.components) filtered.components = doc.components;
        process.stdout.write(JSON.stringify(filtered, null, 2) + "\n");
      }
      process.exit(0);
    });
  return program;
}
